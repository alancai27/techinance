// @ts-check

/**
 * Learn page no longer uses declarative Lucide placeholders for gate icons
 * (those are inline illustrated SVGs). Badge/runtime icons still go through
 * icon.js from learn.js. This file stays as a no-op entry so older build
 * references do not break if anything still imports it.
 */
